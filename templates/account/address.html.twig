{% extends 'base.html.twig' %}

{% block title %}Mes adresses{% endblock %}

{% block content %}
    <a href="{{ path('account_address_add') }}" class="btn btn-info btn-sm
    float-end
    text-white">Ajouter une
        adresse</a>
    <h1>
        Mes adresses
    </h1>
    <p>
        Dans cet espace vous gérez vos adresses.
    </p><br>

    - <a href="{{path('app_account')}}">Retour</a><br>
    <hr>
    {% if app.user.addresses|length == 0 %}
        <p class="text-center">
            Vous n'avez pas encore ajouter d'adresse dans votre compte client
            . Pour en ajouter une, veuillez <a href="{{ path
                ('account_address_add')
            }}">cliquer ici</a>.
        </p>
    {% else %}
        afficher adresses
    {% endif %}

{% endblock %}
